class GameManager {
    grid;

    contructor(width, height){
        this.grid = [];
        for(var x = 0; x < width; x++) {
            grid[x] = [];
            for(var y = 0; y < height; y++) {
                grid[x][y]
            }
        }
    }
}
